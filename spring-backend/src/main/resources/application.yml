spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:./spring-backend/.env[.properties]
      - optional:file:../.env[.properties]
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/student_platform}
server:
  port: ${PORT:5003}

app:
  jwt:
    secret: ${JWT_SECRET:6i6dwD3Fkj5HcXf0R7BgyTHj0aE22l2oWl7w0eb5yL7eWwL9xF4XEtz9VbXlA0rV}
    expiration-days: ${JWT_EXPIRATION_DAYS:7}
  cors:
    allowed-origins:
      - ${FRONTEND_URL:http://localhost:5173}
      - http://127.0.0.1:5173
      - http://localhost:5174
      - http://127.0.0.1:5174
    allowed-origin-patterns:
      - http://localhost:*
      - http://127.0.0.1:*
      - ${VERCEL_ORIGIN_PATTERN:https://*.vercel.app}
  seed:
    enabled: ${SEED_DEMO_DATA:false}
    admin-email: ${ADMIN_EMAIL:gajananmagar.11.01@gmail.com}
    admin-password: ${ADMIN_PASSWORD:321123}
    admin-name: ${ADMIN_NAME:Gajanan Admin}
  ai:
    enabled: ${AI_QUIZ_ENABLED:false}
    openai-api-key: ${OPENAI_API_KEY:}
    openai-model: ${OPENAI_MODEL:gpt-4o-mini}
    openai-base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
    max-question-count: ${AI_MAX_QUESTION_COUNT:15}
